/**
 * Array Functions
 */

// Ex
const numbers = [1, 2, 3];
const doubleNumArray = numbers.map((num) => num * 2); // map function will create a new array

console.log(numbers); // [1, 2, 3]
console.log(doubleNumArray); // [2, 4, 6]
